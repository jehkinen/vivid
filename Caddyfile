{
	http_port {$DEV_HTTP_PORT:8080}
	https_port {$DEV_HTTPS_PORT:443}
	auto_https disable_redirects
	servers {
		protocols h1 h2
	}
	log {
		level ERROR
	}
}

{$DEV_HOST:vivid.test} {
	tls internal
	reverse_proxy 127.0.0.1:{$DEV_PORT:3000}
}
